#include "main.h"

/**
 * _strncat - as name implies
 * @dest: the number to consider
 * @src: the source of the char
 * @n: the number of bytes to use
 * Return: 0 for true, 0 for false
 */
char *_strncat(char *dest, char *src, int n)
{
	return (0);
}

/**
 * _strncpy - as name implies
 * @dest: the number to consider
 * @src: the source of the char
 * @n: the number of bytes to use
 * Return: 0 for true, 0 for false
 */
char *_strncpy(char *dest, char *src, int n)
{
	return (0);
}

/**
 * _strcmp - as name implies
 * @s1: the number to consider
 * @s2: the char to compare with
 * Return: 0 for true, 0 for false
 */
int _strcmp(char *s1, char *s2)
{
	return (0);
}

/**
 * _memset - as name implies
 * @s: the number to consider
 * @b: the second char
 * @n: the unsigned int
 * Return: 0 for true, 0 for false
 */
char *_memset(char *s, char b, unsigned int n)
{
	return (0);
}

/**
 * _memcpy - as name implies
 * @dest: the number to consider
 * @src: the source of the char
 * @n: the number of bytes to copy
 * Return: 0 for true, 0 for false
 */
char *_memcpy(char *dest, char *src, unsigned int n)
{
	return (0);
}
