#include "main.h"

/**
 * _strlen - as name implies
 * @s: the number to consider
 * Return: 0 for true, 0 for false
 */
int _strlen(char *s)
{
	return (0);
}

/**
 * _puts - as name implies
 * @s: the number to consider
 */
void _puts(char *s)
{
}

/**
 * _strcpy - as name implies
 * @dest: the number to consider
 * @src: the source of char
 * Return: 0 for true, 0 for false
 */
char *_strcpy(char *dest, char *src)
{
	return (dest);
}

/**
 * _atoi - as name implies
 * @s: the number to consider
 * Return: 0 for true, 0 for false
 */
int _atoi(char *s)
{
	return (0);
}

/**
 * _strcat - as name implies
 * @dest: the number to consider
 * @src: the source of the char
 * Return: 0 for true, 0 for false
 */
char *_strcat(char *dest, char *src)
{
	return (dest);
}
