#include "main.h"

/**
* mul - function to multiply two numbers
* @a: first number
* @b: second number
* Return: the multiplication value, just shortened
*/
int mul(int a, int b)
{
	return (a * b);
}
