#include <stdio.h>

/**
 * main - first function to run
 * @num: number of argument
 * @argv: list of arguments of size argc
 * Return: 0 for success
 */
int main(int num, char *argv[])
{
	num = 0;

	printf("%s\n", argv[num]);
	return (0);
}
